import PropTypes from 'prop-types'
import {Flex} from '@mantine/core'

export default function Dashboard() {
    return (
        <Flex>
            Dash
        </Flex>
    );
}

Dashboard.proptype = {}